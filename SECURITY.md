# 安全策略

BitCat 是一个**高权限 AI 桌面伴侣**：它可以（在你显式授权后）观察屏幕、执行 shell、读文件、模拟键鼠。我们认真对待这份权限带来的责任。

## 报告漏洞

**请不要用公开 issue 报告安全问题。**

请使用 GitHub 的[私有漏洞报告](https://github.com/gqy20/bitcat/security/advisories/new)（Security → Report a vulnerability）提交，会在确认后尽快回复。

包含以下信息能加快定位：

- 影响的版本（Release 页可查）
- 复现步骤 / 概念验证
- 影响评估（例如：权限边界绕过、提权、敏感数据泄漏）

## 支持版本

| 版本 | 支持状态 |
|------|----------|
| 最新 Release | ✅ |
| 更早版本 | ❌ 请升级后重试 |

## 权限与隐私边界（当前设计承诺）

这些是设计层面的安全承诺，若发现违背欢迎报告：

- **观察类能力默认关闭**：截图观察、摄像头观察、shell、文件读取、剪贴板、热键、前台控制在首次启动由用户亲手开启，随时可收回
- **数据只存本地**：记忆、截图、摄像头记录、日志、提醒全部在 `~/.bitcat/`（或系统数据目录），不内置遥测 / 分析 / 上报服务
- **摄像头观察禁止身份识别**：提示词层明确禁止人脸身份识别和敏感属性推断
- **危险命令拦截**：shell 类工具经过 PermissionHook 安全边界，高危操作被跳过并给出可解释结果
- **AI 内容边界**：小游戏等场景只接收安全投影（短标题/类别/权重），拿不到隐私正文和真实控制权
