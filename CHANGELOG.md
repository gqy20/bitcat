# 8Bit Cat — 更新日志

## 核心功能

### 桌面宠物

像素风小猫住在你的屏幕上，支持拖拽移动、贴边吸附（弹簧缓动动画）、折叠/展开、置顶切换。6 种状态（闲置/行走/睡觉/说话/开心/困惑）带帧动画和粒子特效。

### AI 对话

基于 Anthropic Claude 的 AI Agent，支持流式回复、Tool Use（启动程序、执行命令、读文件、查截图历史）。对话记忆滚动窗口（默认 20 条），持久化到本地。

### 手柄驱动

SDL2 游戏手柄（8BitDo/Xbox/DualSense 等）作为主输入设备，80ms tick 采样。智能设备选择（黑白名单过滤键鼠接收器），支持热插拔自动重连。方向键映射为鼠标滚轮。

### 语音输入

按住手柄按钮触发录音，利用输入法语音识别注入文本，generation 机制防止残留。识别结果送 AI 对话。

### TTS 朗读

Windows SAPI 语音合成，AI 回复后自动朗读，支持切换语音角色。

### 截图观察

独立后台线程定时截图（默认 30s），BitBlt 捕获 + dHash 去重 + 熄屏检测，Vision API 分析屏幕内容并展示在气泡中。支持多显示器拼接。

### 弹出面板

Quicker 风格的 3×2 网格面板，手柄方向键导航，支持快速启动程序和执行脚本。

### 配置系统

- `actions.yml` — 按键→动作绑定（热键/启动程序/脚本/语音）
- `buttons.yml` — 按钮名映射
- `prompts.yml` — AI 人设、视觉提示词、记忆参数

所有配置编译时嵌入 exe（单文件可运行），用户放 yml 到 exe 同目录即可覆盖。
